# Semaphore Account Smart Wallet

### Setup

Before running tests, populate the .env file (see example).

Run the tests with:

```shell
yarn hardhat test --network mumbai
yarn hardhat test --network goerli
```
